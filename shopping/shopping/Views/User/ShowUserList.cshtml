
@model shopping.ViewModels.UserList
@{
    ViewData["Title"] = "User";
}
<h2>@ViewData["Title"]</h2>
<ul>
    @foreach (var user in Model.users) 
    {
    <li>@user.Name</li>
    }
</ul>

<p>Use this area to provide additional information.</p>
